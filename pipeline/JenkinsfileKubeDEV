node {
        stage( 'HelloA' ) {
            echo 'Hello from branch ' + scm.branches[0].name 
            sh( 'env' )
            sh( 'set' )
        }

        stage( 'Checkout' ) {
            checkout scm
        }
}